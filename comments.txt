Привет, Егор,

Хмм, тогда непонятно... у меня все тесты проходят только при ROW_LENGTH + 2
При ROW_LENGTH + 1 падают test_sell_car, test_update_vin, test_top_3_models_by_sales, мои методы возвращают None вместо объекта

добавил _update_index общий метод для всех индексов, но не знаю как отсортировать индексы по первому полю, 
т.к. он был у всех разный (vin, model_id, car_vin), поэтому переименовал первое поле всех индексов как id
